/** @type {import('next').NextConfig} */
// const withTM = require('next-transpile-modules')(['react-infinite-text-reel']);
const nextConfig = {
  reactStrictMode: false,
}

// module.exports = withTM({ reactStrictMode: false});
module.exports = nextConfig
